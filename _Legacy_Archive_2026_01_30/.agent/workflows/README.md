# Workflows

This directory contains workflow definitions used by the AI agent to perform complex tasks consistently.

## Format

Workflows are defined in Markdown files with YAML frontmatter.

```markdown
---
description: [Short description of the workflow]
---
[Step-by-step instructions]
```

## Usage

The agent can read these workflows to understand how to perform specific processes, such as deployment, testing sequences, or new component creation.
